apiVersion: v1
kind: Service
metadata:
  name: bigtablelite-service
  labels:
    app: bigtablelite
spec:
  type: ClusterIP
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
  selector:
    app: bigtablelite

